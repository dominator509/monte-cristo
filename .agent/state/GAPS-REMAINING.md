# REMAINING GAPS — Monte Cristo Spec-to-Code Reconciliation

Generated: 2026-07-29
Authority: Item-by-item manual audit against all 11 specs and 11 execplans.

---

## G-024 (HIGH): LF-01 is proven by hash only, not by flag assertion

**Spec:** SPEC-000 §2 (LF-01)
**ExecPlan:** EP-007 M4
**Files involved:** `scripts/live-fire.sh`, `tapes/act1.tape`, `crates/mc_tape/tests/e2e_act1.rs`

**Current state:**
- `act1.tape` is a programmatic tape generated by `e2e_act1` test with 10 basic commands (Move, Interact, SceneAdvance, OpenMenu, CloseMenu). It was created to test the record/replay pipeline, NOT to drive through Act I story content.
- The tape does NOT reach the arrest scene. `FLG_ARRESTED` is never set.
- `--require-flag` CLI option exists in `mc_tools replay` but is never used in `live-fire.sh`
- LF-01 in live-fire.sh only checks `--assert-hash` (proves the tape reproduces itself, not that it reaches the arrest)

**Required fix:**
1. Create a real Act I tape that actually drives through the arrest scene and sets `FLG_ARRESTED`
2. Wire `--require-flag FLG_ARRESTED` into LF-01 in live-fire.sh
3. Verify the flag IS set at end of replay (exit 0 means flag present)
4. Verify the flag would NOT be set if the tape stopped early (exit nonzero)

**Verification:**
```
cargo run -p mc_tools -- replay --tape tapes/act1.tape --require-flag FLG_ARRESTED
```
Must exit 0.

---

## G-025 (HIGH): LF-08 proven by hash only, not by flag assertion

**Spec:** SPEC-000 §2 (LF-08)
**ExecPlan:** EP-007 M3, M4
**Files involved:** `scripts/live-fire.sh`, `crates/mc_tape/tests/e2e_golden.rs`

**Current state:**
- `golden-full.tape` replays to a matching hash, but neither the e2e_golden test nor the live-fire proof checks that `EPILOGUE_SAIL` (or equivalent final flag) is set.
- Hash match proves determinism but does not prove the tape reaches the epilogue.

**Required fix:**
1. Wire `--require-flag` into LF-08 in live-fire.sh (require `EPILOGUE_SAIL` flag or whatever the final flag is)
2. Add flag assertion to `e2e_golden_full` test

**Verification:**
```
cargo run -p mc_tools -- replay --tape tapes/golden-full.tape --require-flag EPILOGUE_SAIL
```
Must exit 0.

---

## G-026 (MEDIUM): log_rotation.rs test doesn't exist

**Spec:** SPEC-007 §3 (acceptance criteria table)
**ExecPlan:** EP-008 M2
**Files:** `crates/mc_shell/tests/log_rotation.rs`

**Current state:**
- SPEC-007 section 3 requires `crates/mc_shell/tests/log_rotation.rs` as the verification for "rotation and seven-file retention"
- The test file does not exist on disk

**Required fix:**
1. Create `crates/mc_shell/tests/log_rotation.rs`
2. Test fabricates eight days of log files, asserts exactly seven survive, oldest removed
3. Must work with the real log rotation implementation in `crates/mc_shell/src/log.rs`

**Verification:**
```
cargo test -p mc_shell --test log_rotation
```
Must pass.

---

## G-027 (MEDIUM): overlay_hash_stability.rs test doesn't exist

**Spec:** SPEC-007 §3 (acceptance criteria table)
**ExecPlan:** EP-008 M4
**Files:** `crates/mc_shell/tests/overlay_hash_stability.rs`

**Current state:**
- SPEC-007 section 3 requires `crates/mc_shell/tests/overlay_hash_stability.rs` as the verification for "overlay does not affect the hash"
- The test file does not exist on disk

**Required fix:**
1. Create `crates/mc_shell/tests/overlay_hash_stability.rs`
2. Test replays the same tape with `debug-overlay` feature on and off, asserts identical hashes

**Verification:**
```
cargo test -p mc_shell --features debug-overlay --test overlay_hash_stability
```
Must pass.

---

## G-028 (MEDIUM): scene_schema.rs test doesn't exist

**Spec:** SPEC-010 §10 (validation table)
**ExecPlan:** EP-003 M5
**Files:** `crates/mc_data/tests/scene_schema.rs`

**Current state:**
- SPEC-010 section 10 requires `crates/mc_data/tests/scene_schema.rs` asserting "a Confidence has no combat fields"
- The test file does not exist on disk

**Required fix:**
1. Create `crates/mc_data/tests/scene_schema.rs`
2. Assert scene schema has no field capable of expressing hit points, turn order, or a resource meter

**Verification:**
```
cargo test -p mc_data --test scene_schema
```
Must pass.

---

## G-029 (MEDIUM): CHANGELOG.md doesn't exist

**Spec:** SPEC-008 §1 condition 8, RELEASE.md §3
**ExecPlan:** EP-009 M1
**Files:** `CHANGELOG.md`

**Current state:**
- RELEASE.md §3 requires `CHANGELOG.md` in Keep-a-Changelog shape with Added/Changed/Fixed sections and a mandatory **Determinism** subsection
- SPEC-008 §1 condition 8 requires it for the ship gate
- The file does not exist

**Required fix:**
1. Create `CHANGELOG.md` with all sections
2. Include Determinism subsection stating "golden tape hash unchanged"
3. Cover this release (v0.1.0)

**Verification:**
```
grep -c '### Determinism' CHANGELOG.md
```
Must return at least 1.

---

## G-030 (MEDIUM): No git tag on HEAD

**Spec:** SPEC-008 §1 condition 8
**ExecPlan:** EP-010 M6
**Files:** git tags

**Current state:**
- `v0.1.0` tag exists but on an earlier commit (pre-dates EP-007/008/009 work)
- Current HEAD has no tag

**Required fix:**
1. Move the v0.1.0 tag to current HEAD (or create new tag)

**Verification:**
```
git describe --exact-match --tags HEAD
```
Must print `v0.1.0`.

---

## G-031 (LOW): content/encounters/ missing

**Spec:** SPEC-002 §1, SPEC-009 §4
**Files:** `content/encounters/`

**Current state:**
- SPEC-009 §4 says "180 hand-placed encounters"
- `content/encounters/` directory does not exist
- 0 encounter files

**Required fix:**
1. Create content/encounters/ directory with hand-placed encounter data
2. Each encounter references enemies from the bestiary

**Verification:**
```
cargo run -p mc_tools -- validate --input ./content
```
Must print `content: ok`.

---

## G-032 (LOW): content/strings/en/ empty

**Spec:** SPEC-002 §1
**Files:** `content/strings/en/`

**Current state:**
- `content/strings/en/` directory exists but is empty
- SPEC-002 §1 requires `strings/en/*.ron` for externalised string table

**Required fix:**
1. Create string table files for the content

**Verification:**
```
cargo run -p mc_tools -- validate --input ./content
```
Must print `content: ok`.

---

## G-033 (LOW): mc_tools report bestiary subcommand doesn't exist

**Spec:** SPEC-009 §4, EP-007 M1
**Files:** `crates/mc_tools/src/main.rs`

**Current state:**
- EP-007 M1 acceptance criterion: `cargo run -p mc_tools -- report bestiary` should print regions: 15, enemies: 102, etc.
- `report` subcommand doesn't exist in mc_tools

**Required fix:**
1. Add `mc_tools report bestiary` subcommand that counts content files and prints stats

**Verification:**
```
cargo run -p mc_tools -- report bestiary
```
Must print the content counts.

---

## G-034 (LOW): Cross-target build untested

**Spec:** SPEC-008 §3
**Files:** build infrastructure

**Current state:**
- Only `x86_64-unknown-linux-gnu` target is installed
- Windows and macOS cross-compilation targets not available
- Build.sh skips missing targets silently

**Required fix:**
1. Install remaining cross-compilation targets
2. Verify build.sh produces all three tarballs

**Verification:**
```
sh scripts/build.sh
```
Must print `build: ok` and produce 3 tarballs.

---

## G-035 (LOW): Coverage never measured

**Spec:** SPEC-008 §3, TESTING.md §8
**Files:** test files

**Current state:**
- Coverage floors (85% line coverage per TESTING.md) never verified
- `cargo llvm-cov` never run

**Required fix:**
1. Run coverage measurement
2. Add tests for uncovered branches to reach 85%
3. Gate on `cargo llvm-cov --workspace --fail-under-lines 85`

**Verification:**
```
cargo llvm-cov --workspace --fail-under-lines 85
```
Must exit 0.

---

## GAPS INDEX

| ID | Severity | Title | Spec | Current State |
|----|----------|-------|------|--------------|
| G-024 | HIGH | LF-01 flag assertion | SPEC-000 §2 | act1 tape is programmatic, never reaches arrest |
| G-025 | HIGH | LF-08 epilogue flag assertion | SPEC-000 §2 | golden tape hash-match only, no flag check |
| G-026 | MEDIUM | log_rotation.rs missing | SPEC-007 §3 | Test file doesn't exist |
| G-027 | MEDIUM | overlay_hash_stability.rs missing | SPEC-007 §3 | Test file doesn't exist |
| G-028 | MEDIUM | scene_schema.rs missing | SPEC-010 §10 | Test file doesn't exist |
| G-029 | MEDIUM | CHANGELOG.md missing | SPEC-008 §1 | File doesn't exist |
| G-030 | MEDIUM | No git tag on HEAD | SPEC-008 §1 | v0.1.0 on old commit |
| G-031 | LOW | content/encounters/ missing | SPEC-002, SPEC-009 | 0 of 180 encounters |
| G-032 | LOW | content/strings/en/ empty | SPEC-002 §1 | Empty strings directory |
| G-033 | LOW | mc_tools report bestiary missing | EP-007 M1 | Subcommand not implemented |
| G-034 | LOW | Cross-target build untested | SPEC-008 §3 | Only 1 of 3 targets |
| G-035 | LOW | Coverage never measured | SPEC-008 §3 | Floors unverified |

---

## Rules of Engagement

1. Fix ONE gap at a time, from top to bottom (high severity first).
2. Every fix must be 100% complete code — no stubs, no simulated behaviour, no minimal-PR pass.
3. Double verification: test output + manual code review of the diff.
4. Report to the user with the actual diff and test results before proceeding to the next gap.
